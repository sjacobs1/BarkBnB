import {
  View,
  Text,
} from "react-native";
import style from "../pageStyleSheets/addAPetFormStyleSheet";

const AddAPet = () => {

  return (
    <View style={style.mainContainer}>
      <Text>Add A Pet Form</Text>
    </View>
  );
};

export default AddAPet;
